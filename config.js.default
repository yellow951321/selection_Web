const config = {
  'database': {
    'host': '127.0.0.1',
    'port': 3306,
    'user': '',
    'password': '',
  },
  'server': {
    'domain': 'localhost',
    'port': 4430,
    'secret': 'deepest and darkest secret',
    get host() {
      return `https://${config.server.domain}:${config.server.port}`
    }
  },
  'projectRoot': '',
}

module.exports = config
