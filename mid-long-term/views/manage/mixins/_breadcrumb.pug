.breadcrumb
	.ui.huge.breadcrumb
		a.section(href=`/auth/channel`)=user
		- let route = GLOBAL.config.host
		each value, index in breadcrumb
			.divider /
			- route = route + `/${value.id}`
			if index != breadcrumb.length - 1
				a.section(href=route)= value.name
			else
				.section=value.name
